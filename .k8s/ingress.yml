apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: obvestila
  namespace: production
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    kubernetes.io/ingress.class: "gce"
spec:
  tls:
  - hosts:
    - obvestila.prpo.timhrovat.com
    secretName: obvestila-tls
  rules:
  - host: obvestila.prpo.timhrovat.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: obvestila
            port:
              number: 8080
