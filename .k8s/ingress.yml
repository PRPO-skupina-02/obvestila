apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: obvestila
  namespace: production
  annotations:
    kubernetes.io/ingress.class: "gce"
spec:
  tls:
  - hosts:
    - obvestila.cinecore.timhrovat.com
    secretName: obvestila-tls
  rules:
  - host: obvestila.cinecore.timhrovat.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: obvestila
            port:
              number: 8080
